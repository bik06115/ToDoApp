@page
@model ToDoApp.Pages.Presentation.ToDoCategories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ToDoCategory[0].ToDo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToDoCategory[0].Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ToDoCategory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ToDo.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ToDoCatId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ToDoCatId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ToDoCatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
